import styled from '@emotion/styled';
import { IconButton } from '@mui/material';


export const BackButton = styled(IconButton)({
  width: 35,
  height: 35,
  backgroundColor: 'rgba(255,255,255,0.8)',
  boxShadow: '0px 0px 4px 0px rgba(0,0,0,0.36)',
});