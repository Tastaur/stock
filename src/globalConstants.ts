export enum STATUS {
  INITIAL = 'initial',
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILURE = 'failure',
}

export const FAKE_TIMER = 1000;

export enum PAGINATION_PARAMS {
  limit = 'limit',
  offset = 'offset',
}